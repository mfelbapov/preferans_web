defmodule PreferansWeb.Engine do
  @moduledoc """
  Placeholder module for the C++ game engine integration.

  Will manage communication with the C++ Preferans engine via Erlang Port
  (JSON over stdin/stdout) once the C++ side implements the protocol.
  """
end
